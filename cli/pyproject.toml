[tool.poetry]
name = "fy_cli"
version = "0.1.0"
description = ""
authors = ["Vinko Buble <vinkobuble@gmail.com>","Jere Perisic <jereperisic@icloud.com>"]
readme = "README.md"

[tool.poetry.scripts]
fy = "fy_cli.main:main"

[tool.poetry.dependencies]
python = ">=3.10"

fy_library = { path = "../library", develop = true }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
