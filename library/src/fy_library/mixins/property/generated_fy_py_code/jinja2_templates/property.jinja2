class {{ python_class_name.pascal_case }}
{%- if abstract_property_mixins -%}
(
    # Property_mixins
{%- for abstract_property in abstract_property_mixins %}
    {{ abstract_property.python_class_name.pascal_case }},
{%- endfor %}
    {%- if generics_def %}
    Generic[{{ generics_def }}],
    {%- endif %}
    abc.ABC,
)
{%- endif %}
    {%- if generics_def and not abstract_property_mixins -%}
(
    Generic[{{ generics_def }}],
)
{%- endif %}:
    @cached_property
    def _{{ property_name.snake_case }}(self) -> {{ property_type }}:

